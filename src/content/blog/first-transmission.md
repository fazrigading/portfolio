---
title: "First Transmission"
slug: "first-transmission"
date: "2026-09-01"
tags: ["meta", "dedsec"]
excerpt: "New shell, same operator. Why this portfolio is now a static broadcast."
---

New shell, same operator. The old single-page app is archived — this site is now a static broadcast: faster, route-per-beat, no client JS except two islands.

```bash
$ uptime --site
ONLINE since 2026 // 6 routes // 0 frameworks shipped by default
```

What lives where: `/projects` payloads, `/research` archive, `/blog` you-are-here. Ping me on `/contact`.
